# Optional dependencies — install what you need:
#   pip install -r requirements-optional.txt

# OCR (searchable PDF text layer)
ocrmypdf>=15.0.0
pytesseract>=0.3.10
pdf2image>=1.16.0

# Faster async event loop (macOS/Linux, not available on Windows)
uvloop>=0.19.0; sys_platform != "win32"
